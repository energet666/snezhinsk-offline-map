# Оффлайн-карта Снежинска

Полностью автономное веб-приложение с картой Снежинска (Челябинская область):
- режим **«Карта»** — улицы, дома с номерами, здания (данные OpenStreetMap)
- режим **«Спутник»** — снимки Esri World Imagery
- слой **«Названия и объекты»** (улицы, номера домов) можно включить поверх спутника (как гибридный режим Google Maps)
- **организации** не загромождают карту подписями — появляются в попапе по клику на здание
- слой **«Парковки»** — отдельный чекбокс, площадки и точки парковок
- поиск по улицам, номерам домов и организациям (включая те, что не попали в контур здания)

Всё работает без интернета после первого запуска — данные и тайлы уже скачаны и лежат локально.

## Запуск

```bash
cd /root/playground/map-offline
python3 server.py
```

Откройте http://localhost:8765 в браузере.

Сервер — обычный `http.server`, интернет ему не нужен: все ресурсы (Leaflet, GeoJSON-данные, тайлы) уже находятся в этой папке.

## Структура

- `data/*.geojson` — улицы, здания (с номерами домов), организации/POI, парковки, водоёмы, зелень, ж/д — выгружены из OpenStreetMap (Overpass API) в границах Снежинска. Организация попадает в попап здания, если её точка лежит внутри контура этого здания (point-in-polygon); иначе она доступна только через поиск.
- `tiles/{z}/{x}/{y}.png` — растровые спутниковые тайлы Esri World Imagery, зумы 10–17 (~5800 тайлов, 86 МБ), покрывают город с запасом на всех зумах 12–17 одинаково (раньше зона покрытия сужалась на высоких зумах — при увеличении карты пропадали объекты на окраинах; докачано `scratchpad/download_tiles.py`, см. историю сессии).
- `lib/` — локальная копия Leaflet 1.9.4 (карта не зависит от внешних CDN).
- `index.html`, `app.js`, `style.js`, `style.css` — само приложение.
- `server.py` — локальный сервер на стандартной библиотеке Python.

## Ограничения и что можно улучшить

- **Покрытие данных** ограничено окрестностями города (буфер ~2–4 км за границей застройки) — этого достаточно для города и подъездов к нему, но не для области целиком.
- **Источник организаций — только OSM.** Пробовали получать организации через API 2GIS: технически он требует платный ключ, а лицензионное соглашение 2GIS (law.2gis.ru/licensing-agreement, п. 5.1.2, 5.1.4, 5.1.8) прямо запрещает извлекать данные из их баз и строить на них собственные сервисы — соответственно, для этого проекта не подходит. Полнота организаций зависит от того, что замаплено в OSM для Снежинска (699 → 690 после чистки от уличной мебели типа скамеек, добавлены healthcare/craft/tourism/leisure-категории).
- **Телефон/часы работы/сайт** показываются в попапе здания, если это есть в OSM — таких данных мало: из 690 организаций телефон указан у 96, часы работы — у 133, сайт — у 104. Это ограничение исходных данных, не приложения.
- **Названия улиц** в OSM указаны не для всех проездов (170 из 1609 линий дорог имеют name) — это ограничение исходных данных OSM для Снежинска, а не приложения.
- **Спутник**: максимальный явно скачанный зум — 17 (~0.6–1 м/пиксель на этой широте), при увеличении дальше Leaflet будет растягивать эти же тайлы (`maxNativeZoom`). Если нужна более высокая детализация — можно перезапустить `download_tiles.py` (в истории сессии) с `MAX_Z=18` для более точной зоны.
- **Спутник — источник данных**: Esri World Imagery, лицензия допускает офлайн-использование в некоммерческих целях. Прямое массовое скачивание тайлов Google Maps для офлайна нарушает условия использования Google, поэтому не использовалось.
- Для актуализации данных (новые дома/организации) нужно заново выполнить Overpass-запрос и обновить `data/*.geojson`.
